alert（location）直接打印，获取的是地址栏的信息（当前页面完整路径）


直接修改location属性为一个完整路径，或相对路径，则页面自动跳转到该路径

location属性中
把地址分成几个部分，例如location.post设置或返回当前URL的端口号

方法：
assign()作用   和直接   修改location一样，会生成历史记录

reload()作用  刷新 用于重新加载当前页面，和刷新按钮一样
reload（true）传递true作参数  则会强制  清空缓存刷新

replace（）作用 可以使用新页面替换当前页面
不会生成历史记录